#include<bits/stdc++.h>
using namespace std;

int main(){
  
            // char ch = 'k';  
            // // char* cptr = ch;//ths is wrong in case of charcter only this is only correct in case of array and charracter array
            // cout<<ch<<endl;
            // cout<<&ch<<endl;
            // char* cptr = &ch;//this is correct
            // cout << cptr << endl;
            // cout<<*cptr<<endl;
            
             
             
        // char name[10] = "babbar";
        // cout << name << endl;

        // char* c = "babbar";//will give warning but will run
        // cout << c << endl;

        
          int arr[10] = {1,2,3,4};
        // cout << "Size inside main function. " << sizeof(arr) << endl;
        // cout << arr << endl;
        cout << &arr << endl;

 
    return 0;
}