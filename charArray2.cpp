#include<bits/stdc++.h>
using namespace std;


int main(){

        //  string str;
       //cin >> str;  //it will print only before the space
       //  getline(cin, str); //it will print the full name with sapce also
       //  cout << str<<endl;
 

      //  cout << "length: "<< str.length() << endl;
      //  cout << "isEmpty: " << str.empty() << endl;
      // str.push_back('A');
      // cout << str << endl;
     // str.pop_back();
     //  cout << str << endl;
     //  cout << str.substr(1, 5) << endl;//it will print the string form index 1 to till 5
     //  cout << str << endl;


//     string a = "Love";
//     string b = "loue";
//     if(a.compare(b) == 0) {
//           cout << " a and b are exactly same strings" << endl;
//     }
//       else {
//           cout << "a and b are not same" << endl;
//   }

  
//   string x = "bbbb";
//   string y = "bbbb";
//   cout << x.compare(y) << endl;//if not same give -1 if same the give 0


    // string sentence = "hello Jee kaise ho saare";
    // string target = "Everyone";
    // cout << sentence.find(target) << endl;

//   if(sentence.find(target) == string::npos) {
//           cout << "Not Found" << endl;
//   }

   
        // string str = "This is my First Message";
        // string word = "Babbar";

        // str.replace(11, 9 , "Second"); //11 se add karoo and 9 character ko replace kro
        // cout << str << endl;


        string str = "ABCDEFGHIJKLMNOPQRST";
        str.erase(10,5);//10 index se delete karna suru karo and waha se 5 digit delete karo bus 
        cout << str;

    return 0;
}





